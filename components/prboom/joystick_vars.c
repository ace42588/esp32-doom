#include "doomtype.h"

// Joystick variables that are referenced in PrBoom but not defined
// These are used by the configuration system in m_misc.c

int usejoystick = 0;   // Joystick enable flag
int joyleft = 0;       // Left direction
int joyright = 0;      // Right direction  
int joyup = 0;         // Up direction
int joydown = 0;       // Down direction 